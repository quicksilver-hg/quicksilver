# ThreadSanitizer suppressions
# ============================
#
# https://github.com/google/sanitizers/wiki/ThreadSanitizerSuppressions

# race (TODO fix)
race:LoadVault
race:VaultBatch::WriteHDChain
race:DatabaseBatch
race:zmq::*

# deadlock (TODO fix)

# Intentional deadlock in tests
deadlock:sync_tests::potential_deadlock_detected

# Do not restore race:quicksilver-qt or src/qt/test/* wildcards. They hide
# allotment, mining-model, and GPU-option races. TSan CI does not build
# the GUI (BUILD_GUI defaults off); if you run TSan with the GUI, name
# the specific frame.

# External libraries
race:libzmq

# Intermittent issues
# -------------------
#
# Suppressions that follow might only happen intermittently, thus they are not
# reproducible. Make sure to include a link to a full trace.
